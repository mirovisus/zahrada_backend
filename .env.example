# Copy to .env and replace values before running.
# Generate with: openssl rand -base64 32
APP_JWT_SECRET=replace-with-your-own-base64-encoded-256bit-secret
APP_JWT_TEST_SECRET=replace-with-a-different-base64-encoded-256bit-secret
APP_UPLOAD_DIR=./uploads
